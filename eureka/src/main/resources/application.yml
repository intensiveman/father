spring:
  application:
    name: eureka
server:
  port: 10086
eureka:
  client:
    #register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:10086/eureka/